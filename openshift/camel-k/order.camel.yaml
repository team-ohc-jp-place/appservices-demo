- route:
    id: route-2120
    from:
      uri: kafka:user1.earth.dbo.Orders?brokers=earth-cluster-kafka-bootstrap.integration-demo.svc:9092
#      parameters:
#        brokers: earth-cluster-kafka-bootstrap.integration-demo.svc:9092
#        topic: user1.earth.dbo.Orders
      steps:
        - to:
            uri: atlasmap:file:/etc/camel/resources/order-mapping.adm
        - to:
            uri: kafka:incoming-topic?brokers=earth-cluster-kafka-bootstrap.integration-demo.svc:9092
 #       - kamelet:
 #           parameters:
 #             brokers: earth-cluster-kafka-bootstrap.integration-demo.svc:9092
 #             topic: incoming-topic
 #           name: kafka-not-secured-sink
